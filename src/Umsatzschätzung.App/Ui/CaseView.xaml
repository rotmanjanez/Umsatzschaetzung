<ui:Screen x:Class="Umsatzschätzung.App.Ui.CaseView"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:ui="clr-namespace:Umsatzschätzung.App.Ui"
           Margin="24">
    <ui:Screen.Resources>
        <Style x:Key="Two" TargetType="Grid">
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
    </ui:Screen.Resources>
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="Prüfung" Style="{StaticResource PageTitle}"/>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel MaxWidth="960" HorizontalAlignment="Left" Margin="0,0,16,0">
                <TextBlock Text="Prüfung" Style="{StaticResource SectionHeader}"/>
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Bezeichnung *" Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding Label, UpdateSourceTrigger=PropertyChanged}" Width="440" HorizontalAlignment="Left"/>
                        <TextBlock Text="Zeitraum" Style="{StaticResource SectionHeader}" Margin="0,20,0,0"/>
                        <Grid Style="{StaticResource Two}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Von *" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding From, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Bis *" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding To, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </Grid>
                        <TextBlock Text="Steuerpflichtiger" Style="{StaticResource SectionHeader}" Margin="0,20,0,0"/>
                        <Grid Style="{StaticResource Two}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Name" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Text="PaB-Nr." Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding Pab, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Steuernummer" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding TaxNumber, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <TextBlock Text="Erklärte Umsätze, netto in €" Style="{StaticResource SectionHeader}"/>
                <Border Style="{StaticResource Card}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Umsatz zu 19 %" Style="{StaticResource FieldLabel}"/>
                            <TextBox Style="{StaticResource NumberBox}" Text="{Binding Declared19, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Umsatz zu 7 %" Style="{StaticResource FieldLabel}"/>
                            <TextBox Style="{StaticResource NumberBox}" Text="{Binding Declared7, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="4">
                            <TextBlock Text="Umsatz zu 0 %" Style="{StaticResource FieldLabel}"/>
                            <TextBox Style="{StaticResource NumberBox}" Text="{Binding Declared0, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <TextBlock Text="Bestand" Style="{StaticResource SectionHeader}"/>
                <Border Style="{StaticResource Card}">
                    <StackPanel Grid.IsSharedSizeScope="True">
                        <Grid Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="90"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Zutat" Style="{StaticResource FieldLabel}"/>
                            <TextBlock Grid.Column="1" Text="Anfangsbestand" Style="{StaticResource FieldLabel}" Margin="8,10,0,4"/>
                            <TextBlock Grid.Column="2" Text="Endbestand" Style="{StaticResource FieldLabel}" Margin="8,10,0,4"/>
                        </Grid>
                        <ItemsControl ItemsSource="{Binding Stock}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="140"/>
                                            <ColumnDefinition Width="140"/>
                                            <ColumnDefinition Width="90"/>
                                        </Grid.ColumnDefinitions>
                                        <ComboBox ItemsSource="{Binding Options}" DisplayMemberPath="Name" SelectedItem="{Binding Ingredient}"/>
                                        <TextBox Grid.Column="1" Style="{StaticResource NumberBox}" Margin="8,0,0,0" Text="{Binding Opening, UpdateSourceTrigger=PropertyChanged}"/>
                                        <TextBox Grid.Column="2" Style="{StaticResource NumberBox}" Margin="8,0,0,0" Text="{Binding Closing, UpdateSourceTrigger=PropertyChanged}"/>
                                        <Button Grid.Column="3" Content="&#xE74D;" Style="{StaticResource DangerIcon}" ToolTip="Entfernen" Click="RemoveStock" HorizontalAlignment="Right"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Button Content="Zutat hinzufügen" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Left" Click="AddStock" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}">
                    <Expander x:Name="More" Header="Weitere Angaben">
                        <StackPanel Margin="0,12,0,0">
                            <TextBlock Text="Ertragsregeln" Style="{StaticResource SectionHeader}"/>
                            <TextBlock Text="Keine Ertragsregeln zur Auswahl" Style="{StaticResource Muted}">
                                <TextBlock.Visibility>
                                    <Binding Path="NoYields">
                                        <Binding.Converter>
                                            <BooleanToVisibilityConverter/>
                                        </Binding.Converter>
                                    </Binding>
                                </TextBlock.Visibility>
                            </TextBlock>
                            <ItemsControl ItemsSource="{Binding Yields}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="260"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Label}" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Kind}" Style="{StaticResource Muted}" VerticalAlignment="Center"/>
                                            <ComboBox Grid.Column="2" ItemsSource="{Binding Rules}" DisplayMemberPath="Name" SelectedItem="{Binding Selected}" MaxWidth="360" HorizontalAlignment="Left"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Expander>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</ui:Screen>
